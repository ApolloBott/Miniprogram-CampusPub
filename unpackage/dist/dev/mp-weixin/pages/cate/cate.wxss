@charset "UTF-8";
/* 水平间距 */
/* 水平间距 */
/* ===== 全局样式优化 ===== */
page {
  background-color: #f5f7fa;
}
/* ===== 🔥 顶部导航栏 - 添加动画效果 ===== */
.top-nav-bar {
  position: -webkit-sticky;
  position: sticky;
  top: 0;
  z-index: 999;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12rpx 24rpx;
  background: #ffffff;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  /* 🔥 关键：添加过渡动画 */
  transition: -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  /* 🔥 隐藏状态 */
}
.top-nav-bar.nav-hidden {
  box-shadow: none;
}
/* 🔥🔥🔥 类别选项栏样式 🔥🔥🔥 */
.category-nav-bar {
  position: -webkit-sticky;
  position: sticky;
  top: 88rpx;
  /* 紧贴顶部导航栏下方 */
  z-index: 998;
  background: #ffffff;
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding: 10rpx 14rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  /* 平滑过渡动画 */
  transition: box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1), -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1), -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.category-nav-bar.category-hidden {
  box-shadow: none;
}
.category-scroll {
  width: 100%;
  white-space: nowrap;
}
.category-list {
  display: inline-flex;
  padding: 10rpx 16rpx;
  gap: 24rpx;
}
.category-option {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-width: 120rpx;
  padding: 10rpx 16rpx;
  background: #fff;
  border-radius: 20rpx;
  transition: all 0.3s ease;
  border: 2rpx solid transparent;
}
.category-option .category-label {
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
  white-space: nowrap;
}
.category-option.active {
  font-size: 30rpx;
}
.category-option.active .category-label {
  color: #000;
  font-weight: 600;
  font-size: 30rpx;
}
.category-option:active {
  -webkit-transform: scale(0.95);
          transform: scale(0.95);
  color: #000;
}
/* 响应式优化 */
@media screen and (max-width: 375px) {
.category-option {
    min-width: 60rpx;
    padding: 10rpx 16rpx;
}
.category-option .category-label {
    font-size: 28rpx;
}
}
/* 🔥 菜单按钮样式 - 替换原来的消息按钮 */
.menu-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 72rpx;
  height: 64rpx;
  background: #f5f5f5;
  border-radius: 16rpx;
  transition: all 0.3s ease;
  flex-shrink: 0;
}
.menu-btn:active {
  background: #e8e8e8;
  -webkit-transform: scale(0.95);
          transform: scale(0.95);
}
.menu-btn .menu-icon {
  font-size: 44rpx;
  color: #333;
  font-weight: bold;
}
/* 🔥🔥🔥 侧边菜单遮罩层 🔥🔥🔥 */
.side-menu-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0);
  z-index: 9998;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}
.side-menu-overlay.show {
  opacity: 1;
  visibility: visible;
  background: rgba(0, 0, 0, 0.5);
}
/* 🔥🔥🔥 侧边菜单抽屉 🔥🔥🔥 */
.side-menu-drawer {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  width: 560rpx;
  background: #ffffff;
  box-shadow: 4rpx 0 24rpx rgba(0, 0, 0, 0.15);
  -webkit-transform: translateX(-100%);
          transform: translateX(-100%);
  transition: -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), -webkit-transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  display: flex;
  flex-direction: column;
  z-index: 9999;
}
.side-menu-drawer.show {
  -webkit-transform: translateX(0);
          transform: translateX(0);
}
/* 🔥 菜单头部 */
.menu-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 24rpx;
  background: linear-gradient(135deg, #C00000 0%, #ff4757 100%);
  box-shadow: 0 4rpx 12rpx rgba(192, 0, 0, 0.1);
}
.menu-header .menu-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #ffffff;
}
.menu-header .close-btn {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  transition: all 0.3s ease;
}
.menu-header .close-btn:active {
  background: rgba(255, 255, 255, 0.3);
  -webkit-transform: scale(0.9);
          transform: scale(0.9);
}
.menu-header .close-btn .close-icon {
  font-size: 40rpx;
  color: #ffffff;
  font-weight: bold;
  line-height: 1;
}
/* 🔥 菜单列表 */
.menu-list {
  flex: 1;
  padding: 16rpx 0;
  overflow-y: auto;
}
.menu-item {
  display: flex;
  align-items: center;
  padding: 32rpx 24rpx;
  margin: 8rpx 16rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  transition: all 0.3s ease;
  border: 2rpx solid transparent;
}
.menu-item:active {
  background: #e8ecef;
  -webkit-transform: scale(0.98);
          transform: scale(0.98);
  border-color: #C00000;
}
.menu-item .menu-item-icon {
  font-size: 44rpx;
  margin-right: 20rpx;
  width: 48rpx;
  text-align: center;
}
.menu-item .menu-item-text {
  flex: 1;
  font-size: 30rpx;
  color: #333;
  font-weight: 500;
}
.menu-item .menu-arrow {
  font-size: 48rpx;
  color: #999;
  line-height: 1;
  margin-left: 12rpx;
}
/* 🔥 响应式优化 - 小屏幕 */
@media screen and (max-width: 375px) {
.side-menu-drawer {
    width: 520rpx;
}
.menu-item {
    padding: 28rpx 20rpx;
}
.menu-item .menu-item-icon {
    font-size: 40rpx;
    width: 44rpx;
}
.menu-item .menu-item-text {
    font-size: 28rpx;
}
}
/* 🔥 中间标签区域 */
.nav-tabs {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 40rpx;
  padding: 0 20rpx;
}
.nav-tab-item {
  position: relative;
  padding: 8rpx 20rpx;
  transition: all 0.3s ease;
}
.nav-tab-item .tab-label {
  font-size: 30rpx;
  color: #666666;
  font-weight: 500;
  transition: all 0.3s ease;
}
.nav-tab-item.active .tab-label {
  color: #000;
  font-weight: 700;
  font-size: 30rpx;
}
.nav-tab-item:active {
  -webkit-transform: scale(0.95);
          transform: scale(0.95);
}
/* 🔥 搜索图标按钮 */
.search-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 72rpx;
  height: 64rpx;
  background: #f5f5f5;
  border-radius: 16rpx;
  transition: all 0.3s ease;
  flex-shrink: 0;
}
.search-btn:active {
  background: #e8e8e8;
  -webkit-transform: scale(0.95);
          transform: scale(0.95);
}
.search-btn .search-icon {
  font-size: 36rpx;
}
/* 🔥 分类页面样式 */
.category-page {
  padding: 24rpx;
  background: #f5f7fa;
  min-height: 100vh;
}
.category-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24rpx;
  padding: 0 16rpx;
}
.category-item {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40rpx 24rpx;
  background: #ffffff;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
  border: 4rpx solid transparent;
}
.category-item:active {
  -webkit-transform: scale(0.98);
          transform: scale(0.98);
}
.category-item.active {
  background: linear-gradient(135deg, #fff5f5 0%, #ffe5e5 100%);
  border-color: #C00000;
  box-shadow: 0 6rpx 20rpx rgba(192, 0, 0, 0.15);
}
.category-item.active .category-name {
  color: #C00000;
  font-weight: 700;
}
.category-item.active .category-count {
  color: #C00000;
}
.category-item .category-icon {
  font-size: 64rpx;
  margin-bottom: 20rpx;
}
.category-item .category-name {
  font-size: 28rpx;
  color: #333;
  font-weight: 600;
  margin-bottom: 8rpx;
}
.category-item .category-count {
  font-size: 24rpx;
  color: #999;
}
.category-item .check-indicator {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  width: 40rpx;
  height: 40rpx;
  background: #C00000;
  color: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: bold;
}
/* 🔥 分类下拉菜单 */
.category-dropdown {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.3);
  z-index: 9999;
  display: flex;
  align-items: flex-start;
  padding-top: 96rpx;
}
.category-dropdown .dropdown-content {
  width: 92%;
  max-width: 680rpx;
  max-height: 45vh;
  margin: 0 auto;
  background: #fff;
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
  -webkit-animation: slideDown 0.25s ease;
          animation: slideDown 0.25s ease;
}
@-webkit-keyframes slideDown {
from {
    -webkit-transform: translateY(-20rpx);
            transform: translateY(-20rpx);
    opacity: 0;
}
to {
    -webkit-transform: translateY(0);
            transform: translateY(0);
    opacity: 1;
}
}
@keyframes slideDown {
from {
    -webkit-transform: translateY(-20rpx);
            transform: translateY(-20rpx);
    opacity: 0;
}
to {
    -webkit-transform: translateY(0);
            transform: translateY(0);
    opacity: 1;
}
}
.category-dropdown .dropdown-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 24rpx;
  background: linear-gradient(135deg, #C00000 0%, #C00000 100%);
  box-shadow: 0 2rpx 8rpx rgba(192, 0, 0, 0.1);
}
.category-dropdown .dropdown-header .dropdown-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #fff;
}
.category-dropdown .dropdown-header .close-btn {
  font-size: 36rpx;
  color: #fff;
  line-height: 1;
  padding: 8rpx;
}
.category-dropdown .dropdown-grid-container {
  max-height: calc(45vh - 56rpx);
  padding: 16rpx 12rpx;
}
.category-dropdown .dropdown-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12rpx;
}
.category-dropdown .grid-item {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 20rpx 12rpx;
  background: #f8f8f8;
  border-radius: 16rpx;
  border: 2rpx solid transparent;
  transition: all 0.3s ease;
  min-height: 140rpx;
}
.category-dropdown .grid-item:active {
  -webkit-transform: scale(0.96);
          transform: scale(0.96);
}
.category-dropdown .grid-item.active {
  background: linear-gradient(135deg, #fff5f5 0%, #ffe5e5 100%);
  border-color: #C00000;
  box-shadow: 0 4rpx 12rpx rgba(192, 0, 0, 0.15);
}
.category-dropdown .grid-item.active .item-name {
  color: #C00000;
  font-weight: 700;
}
.category-dropdown .grid-item.active .item-count {
  color: #C00000;
}
.category-dropdown .grid-item .item-icon {
  font-size: 48rpx;
  margin-bottom: 8rpx;
}
.category-dropdown .grid-item .item-name {
  font-size: 24rpx;
  color: #333;
  font-weight: 600;
  text-align: center;
  line-height: 1.3;
  margin-bottom: 4rpx;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.category-dropdown .grid-item .item-count {
  font-size: 20rpx;
  color: #999;
}
.category-dropdown .grid-item .item-count::after {
  content: "件";
}
.category-dropdown .grid-item .check-mark {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  width: 32rpx;
  height: 32rpx;
  background: #C00000;
  color: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20rpx;
  font-weight: bold;
  box-shadow: 0 2rpx 8rpx rgba(192, 0, 0, 0.3);
}
/* 🔥 响应式调整 - 小屏幕改为2列 */
@media screen and (max-width: 375px) {
.category-dropdown .dropdown-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 10rpx;
}
.category-dropdown .grid-item {
    min-height: 130rpx;
}
.category-dropdown .grid-item .item-icon {
    font-size: 44rpx;
}
.category-dropdown .grid-item .item-name {
    font-size: 22rpx;
}
}
/* 🔥 分类商品展示区域 */
.category-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx;
  background: #fff;
  margin: 16rpx 16rpx 0;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}
.category-header .category-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
}
.category-header .goods-count {
  font-size: 26rpx;
  color: #999;
}
/* ===== 瀑布流优化 ===== */
.waterfall-container {
  background: #f5f7fa;
  min-height: 100vh;
}
.waterfall {
  display: flex;
  justify-content: space-between;
  padding: 16rpx;
  box-sizing: border-box;
}
.col {
  width: 48%;
  display: flex;
  flex-direction: column;
}
.goods-wrapper {
  margin-bottom: 16rpx;
  border-radius: 16rpx;
  overflow: hidden;
  background: #fff;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
  transition: all 0.3s ease;
}
.goods-wrapper:active {
  -webkit-transform: translateY(-4rpx);
          transform: translateY(-4rpx);
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.1);
}
/* 🔥 空状态 🔥 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 200rpx 0;
}
.empty-state .empty-icon {
  font-size: 120rpx;
  margin-bottom: 24rpx;
  opacity: 0.5;
}
.empty-state .empty-text {
  font-size: 28rpx;
  color: #999;
}
.empty-state .empty-tip {
  font-size: 26rpx;
  color: #999;
  margin-top: 12rpx;
}
/* 🔥 加载状态 🔥 */
.loading-state {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 40rpx 0;
}
.loading-state .loading-text {
  font-size: 26rpx;
  color: #999;
}
